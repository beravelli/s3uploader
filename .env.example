AWS_REGION=us-east-1
S3_BUCKET=my-photo-bucket
# Credentials resolve via the standard AWS chain (env vars, ~/.aws/credentials, SSO).
# Uncomment to set explicitly:
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
PORT=8080
MAX_UPLOAD_MB=100
